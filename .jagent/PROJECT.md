# water-spider

RunPod GPU pod orchestration CLI — create/connect/tunnel/recipe/gui/snapshot, built on runpodctl + the RunPod GraphQL API

## Identity

- **Repository:** water-spider
- **Language:** Bash
- **Protocol:** Command-line interface over `runpodctl`, SSH, and the RunPod GraphQL API
- **Remote:** `https://github.com/nixpt/water-spider`
- **Documentation:** `docs/README.md`
- **Verification:** `./tests/run.sh` (29 deterministic cases) + ShellCheck 0.10.0

**Working this backlog?** Read `.jagent/planning/RULES.md` first — one worktree/branch per
ticket/milestone + verify-before-fix.
