# water-spider

RunPod GPU pod orchestration CLI — create/connect/tunnel/recipe/gui/snapshot, built on runpodctl + the RunPod GraphQL API

## Status

Freshly scaffolded via `foreman-scaffold` — nothing built yet. See `.jagent/planning/ROADMAP.md`
for the plan and `STATE.md` for current status.
